# Issues

No upstream P0, P1, or P2 defect was established from the published materials used for this rebuild. The upstream repository exposes both training entry points, documents the direct-transcript alternative to pseudo-labels, and computes evaluation metrics on the executed path.

Record-level caveats are kept in `row.json` and the README: no verified hosted experiment URL was produced, and the import-versus-freeze audit found `wandb` missing from the recorded package set. These are not attributed to the upstream project.
